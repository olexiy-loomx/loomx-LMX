{
  "name": "loomx-ui",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Loomx custom web UI — replaces the stock WLED UI",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/bundle-to-header.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^3.1.2",
    "svelte": "^4.2.19",
    "vite": "^5.4.10",
    "vite-plugin-singlefile": "^2.0.3"
  },
  "dependencies": {
    "lucide-svelte": "^0.460.0"
  }
}
